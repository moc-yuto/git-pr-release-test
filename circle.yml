machine:
  timezone:
    Asia/Tokyo

test:
  override:
    - echo test

deployment:
  staging:
    branch: /(deploy\/.*|master)/
    commands:
      - echo staging
  production:
    branch: production
    commands:
      - echo production
      - echo git-pr-release
