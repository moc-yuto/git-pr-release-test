machine:
  timezone:
    Asia/Tokyo

test:
  override:
    - echo test
    - bundle exec git-pr-release --release

deployment:
  staging:
    branch: /(deploy\/.*|master)/
    commands:
      - echo staging
  production:
    branch: production
    commands:
      - echo production
      - bundle exec git-pr-release --release
